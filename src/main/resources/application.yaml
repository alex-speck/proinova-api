spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/cadastro?createDatabaseIfNotExist=true
    username: root
    driverClassName: com.mysql.cj.jdbc.Driver
